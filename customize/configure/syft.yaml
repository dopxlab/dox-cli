
installation:
  download:
    template: "https://github.com/anchore/syft/releases/download/v{version}/syft_{version}_{os}_{arch}.tar.gz"
    mappings:
      os:
        mac: "Darwin"
        linux: "Linux"
      arch:
        x86_64: "amd64"
        aarch64: "arm64"
configuration:
  default_version: "${SYFT_VERSION:-$(curl -s https://api.github.com/repos/anchore/syft/releases/latest | jq -r '.tag_name' | sed 's/v//')}"
  environments:
    PATH: "${install_dir}"
  post_configuration_script: |
    version "Syft Version" "$(syft version)"
